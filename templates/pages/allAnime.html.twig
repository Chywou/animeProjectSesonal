{% extends "base.html.twig" %}
{% block title %}OUIIIIIIIIii{% endblock %}
{% block body %}
<div class="text-center row m-0 mt-3">
                {% for anime in allAnimes %}
                    <div class="card p-2 col-3">
                        <img class="card-img-top" src="https://{{anime.aniImage}}" alt="Card image cap">
                        <div class="card-body p-2">
                            <h5 class="card-title">
                                <a class="text-decoration-none" href="{{ path('oneAnime', {id: anime.id, aniName: anime.aniName}) }}" >{{anime.aniName}}</a>
                            </h5>
                            <p class="card-text">{{anime.aniHour|date('H:i')}}<br>épisode {{anime.aniCurrentNB}}/{{anime.aniFullNB}}</p>
                        </div>
                    </div>
                {% endfor %}
</div>
<div class="navigation d-flex justify-content-center mt-3">
    {{ knp_pagination_render(allAnimes) }}
</div>
{% endblock %}